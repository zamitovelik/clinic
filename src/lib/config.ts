/**
 * Параметры записи на приём. Собраны в одном месте, чтобы при подключении CRM
 * менять их здесь, а не искать по компонентам.
 */

/** Часовой пояс клиники. Ташкент, без переходов на летнее время. */
export const TIMEZONE = "Asia/Tashkent";

/** Шаг сетки времени в минутах. */
export const SLOT_MINUTES = 30;

/** На сколько дней вперёд открыта запись. */
export const BOOKING_HORIZON_DAYS = 21;

/** Записаться можно не позднее чем за столько часов до приёма. */
export const MIN_LEAD_HOURS = 2;

/** Сколько дней показывает календарь одним экраном. */
export const CALENDAR_VISIBLE_DAYS = 14;

/** Задержка мок-запросов, чтобы состояния загрузки были видны при разработке. */
export const MOCK_LATENCY_MS = 220;
